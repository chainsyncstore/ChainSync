#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged with explicit config file path
npx lint-staged --config=".lintstagedrc.js"

# Additional security checks if needed
echo "✅ Pre-commit checks completed successfully"
